# Sanitize column names (internal)

Replace problematic characters in column names to get something closer
to brms' naming scheme.

## Usage

``` r
sanitize_names(x)
```

## Arguments

- x:

  Character vector of names.

## Value

Character vector with sanitized names.
