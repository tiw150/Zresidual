% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Zresidual.hurdle.poisson.brms.R
\name{Zresidual.hurdle_poisson.brms}
\alias{Zresidual.hurdle_poisson.brms}
\title{Z-residuals for hurdle Poisson models fitted with brms}
\usage{
\method{Zresidual}{hurdle_poisson.brms}(
  object,
  nrep = 1,
  data,
  type = c("hurdle", "count", "zero"),
  method = "iscv",
  ...
)
}
\arguments{
\item{object}{A \code{brmsfit} object with hurdle Poisson family
(\code{brms::family(object)$family == "hurdle_poisson"}).}

\item{nrep}{Integer; number of replicated Z-residual sets to generate.
Default is \code{1}.}

\item{data}{Optional data frame used for prediction or residual computation.
If \code{NULL} (default), the data stored inside the \code{brmsfit} object are used.}

\item{type}{Character string specifying which part of the model to
compute Z-residuals for:
\itemize{
\item \code{"zero"}   — the hurdle/zero part;
\item \code{"count"}  — the truncated Poisson count part;
\item \code{"hurdle"} — the full hurdle-Poisson model.
}
The default is \code{"hurdle"}.}

\item{method}{Character string specifying the residual calculation method:
\code{"iscv"} for importance-sampled cross-validated randomized predictive
p-values, \code{"rpost"} for randomized posterior predictive p-values,
or \code{"mpost"} for middle-value posterior predictive p-values.
Default is \code{"iscv"}.}

\item{...}{Further arguments passed to the underlying implementation
function \code{\link[=Zresidual_hurdle_poisson_brms]{Zresidual_hurdle_poisson_brms()}}.}
}
\value{
A numeric matrix of Z-residuals (one column per replication) as returned
by \code{\link[=Zresidual_hurdle_poisson_brms]{Zresidual_hurdle_poisson_brms()}}, with the class \code{"zresid"}
added to its class vector.
}
\description{
\code{Zresidual.hurdle_poisson.brms()} is the S3 method for \code{\link[=Zresidual]{Zresidual()}}
when applied to hurdle Poisson models fitted with \code{\link[brms:brm]{brms::brm()}}
and \code{family = hurdle_poisson()}. Objects are dispatched here when
the fitted object is a \code{"brmsfit"} with
\code{brms::family(object)$family == "hurdle_poisson"} and has been
internally tagged with the class \code{"hurdle_poisson.brms"} by
\code{\link[=Zresidual]{Zresidual()}}.

In normal use, users should call \code{\link[=Zresidual]{Zresidual()}} directly on the
\code{brmsfit} object (for example \code{Zresidual(fit)}), rather than calling
\code{Zresidual.hurdle_poisson.brms()} explicitly.
}
\examples{
\dontrun{
  library(brms)
  fit_hp <- brm(y ~ x1 + x2, data = df,
                family = hurdle_poisson())

  ## Counts part only
  z_count <- Zresidual(fit_hp, type = "count", method = "iscv")

  ## Full hurdle model with 2 replicates
  z_hurdle <- Zresidual(fit_hp, type = "hurdle", nrep = 2)
}

}
