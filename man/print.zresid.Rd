% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.Zresidual.R
\name{print.zresid}
\alias{print.zresid}
\alias{print.cvzresid}
\title{Print methods for Z-residual objects}
\usage{
\method{print}{zresid}(x, ...)

\method{print}{cvzresid}(x, ...)
}
\arguments{
\item{x}{An object returned by \code{\link{Zresidual}} or
\code{\link{CV.Zresidual}}.}

\item{...}{Further arguments passed to \code{print()}.}
}
\value{
The input object, invisibly.
}
\description{
Compact print methods for \code{"zresid"} and \code{"cvzresid"} objects.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  set.seed(1)
  n <- 20
  x <- rnorm(n)
  t_event <- rexp(n, rate = exp(0.2 * x))
  t_cens  <- rexp(n, rate = 0.5)
  status  <- as.integer(t_event <= t_cens)
  time    <- pmin(t_event, t_cens)
  dat <- data.frame(time = time, status = status, x = x)
  fit <- survival::coxph(survival::Surv(time, status) ~ x, data = dat)

  z <- Zresidual(fit, data=dat, nrep = 1, seed = 1)
  print(z)
}

}
